import 'package:fraction/math/Fraction.dart';
import 'package:fraction/model/keys.enum.dart';

class FractionModel {
  final Keys key;
  final Fraction fraction;

  FractionModel(this.key, this.fraction);
}
